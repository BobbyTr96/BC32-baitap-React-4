import data from "../../src/BT_DatVe/data.json";

const initialState = data;

const chairReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default chairReducer;
